
import SailingBoatPreloader from '@/components/ui/sailing-boat-preloader';

export default function Loading() {
  // This now uses the new, custom preloader component.
  return <SailingBoatPreloader />;
}
